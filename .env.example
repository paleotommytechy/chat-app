# Local Convex CLI linkage. npx convex dev manages this in .env.local.
CONVEX_DEPLOYMENT=

# Required by the Next.js frontend locally and in Vercel.
NEXT_PUBLIC_CONVEX_URL=

# No WORKSPACE_ACCESS_CODE is required.
# No Vercel-side encryption secret is required.
